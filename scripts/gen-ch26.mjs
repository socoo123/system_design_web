import { writeChapter } from "./write-chapter.mjs";

const d2 = (src) => {
  const body = src.trim();
  const sized = /style\.font-size/.test(body) ? body : `style.font-size: 12\n${body}`;
  return "```d2\n" + sized + "\n```";
};

writeChapter({
  id: "ch26",
  num: "26",
  title: "红包系统",
  kind: "case",
  relatedChapters: ["ch41", "ch38", "ch24"],
  sections: [
    {
      id: "intro",
      heading: "",
      secNum: null,
      related: [],
      body: [
        "> **预计**：50–60 分钟 ｜ **前置**：支付 Ch24；缓存 Ch38；事务 Ch41",
        "> **目标**：拆红包、库存防超发、过期退回、对账。钱和渠道回支付章，不展开钱包。",
        "",
        "调度把「到点只跑一次」讲完；这题换成 **固定总额怎么拆、怎么不被抢超、剩的怎么退回去**。默认是 **群红包**：总额固定、限个数、随机或均分、过期退剩余。不是钱包选修、不是证券、不是把 Ch24 支付渠道再画一遍——红包站在 **业务库存** 这一侧，钱在支付 / 账本。",
        "",
        "系统看起来就是发一个包、群里抢几下。三个 hard part：**怎么拆（预生成 vs 现场随机）**、**库存原子防超发**、**过期退回 + 和支付对账边界**。面试官要看的不是某晚高峰公开 QPS、不是红包雨运营后台、不是钱包余额双花，而是：N 份金额加起来会不会多出一分；同一 `packet_id` 并发会不会超发；过期那一分钱退回时，库存和账本能不能对上。",
      ].join("\n"),
    },
    {
      id: "sec-answer",
      heading: "面试怎么答",
      secNum: null,
      related: [],
      body: [
        "**开场 30 秒（直接说）：**",
        "",
        "> 「这是群红包，不是钱包、不是证券、不是完整支付渠道。三个 hard part：怎么拆、库存原子防超发、过期退回和对账边界。总额固定、限个数、拼手气或均分、过期退剩余。2026 默认 **发时预生成**（二倍均值 + 打乱），抢时 Redis Lua 原子预扣 **剩余个数 + 剩余金额**。高峰不要锁 DB 行。钱的冻结、入账、退款走 Ch24；红包只做业务库存。落库 Outbox，对账把库存和账本对齐。过期任务点到 Ch25。」",
        "",
        "然后按 4 步走，别一上来画红包雨中台、钱包账户、渠道清算、某厂 SET 化。",
        "",
        d2(`
direction: right
s1: "1 澄清范围"
s2: "2 粗估热点"
s3: "3 高层库存"
s4: "4 拆包对账"
s1 -> s2 -> s3 -> s4
`),
        "",
        "| 时间盒 | 你在做什么 |",
        "|---|---|",
        "| 3–10 min | 澄清：群红包 vs 个人/红包雨、随机 vs 均分、预生成 vs 现场、过期、和 Ch24 怎么切 |",
        "| 接着 2 min | back-of-envelope：日发包教学假设；怕的是单包热点超发，不是全国总和 |",
        "| 10–15 min | 高层：Client → Packet API → Redis 库存；钱不进这张图 |",
        "| 10–25 min | deep dive：预生成 vs 现场随机、Lua 原子预扣、过期退回 + 支付对账 |",
        "| 3–5 min | wrap-up：3 个 bottleneck（拆不平、超发、退回和对不上账） |",
        "",
        "**red flag：** 还没问范围就画钱包 / 证券；高峰 `FOR UPDATE` 锁红包行；只 DECR 个数不管金额；上来和支付做 2PC；把 Ch24 账本科目画进第一张图；拿某晚高峰 QPS 当自己的事实。那是 over-engineering，或把邻章 / 选修整段搬进来。",
      ].join("\n"),
    },
    {
      id: "sec-clarify",
      heading: "澄清问题清单",
      secNum: "26.1",
      related: ["ch24"],
      body: [
        "没问清楚就画图 = Jimmy。红包这题 5–7 个问题就停，其余自己假设写白板。**第一问必须是范围。**",
        "",
        "| 你问 | 典型回答 / 你自己的假设 | 它改什么 |",
        "|---|---|---|",
        "| 群红包、个人红包，还是红包雨？ | **群红包**（固定总额、限个数） | 个人=1 个，无拆包；红包雨是运营投放，本场不画 |",
        "| 拼手气还是均分？ | **拼手气为主**；均分点到 | 均分仍要原子扣个数 |",
        "| 金额预生成，还是抢的时候随机？ | **预生成（默认）** | 现场二倍均值当对照，见表 |",
        "| 过期多久？没领完怎么办？ | 教学 **24h**，剩余退回发送方 | 调度点到 Ch25；退款出口 Ch24 |",
        "| 钱在哪？要不要画钱包？ | **支付冻结 / 入账 / 退款** | 钱包选修暂停；不重写渠道 |",
        "| 已有 Redis，还是只有 DB？ | **已有 Redis + 红包表** | 高峰库存走 Lua，不锁 DB 行 |",
        "",
        "面试官说「你定」时，把假设写上去：",
        "",
        "> 「我假设：群红包，固定总额、限个数，拼手气。发时二倍均值预生成并打乱，灌 Redis List。抢时 Lua 原子扣剩余个数和剩余金额，`(packet_id, user_id)` 每人一次。金额整数分。支付成功才开放抢；过期剩余走 Ch24 退款。先按这个画，不对你打断我。」",
        "",
        "问到钱包 / 证券 / 红包雨 / 完整支付渠道：**承认差别，立刻收口。** 「钱包是账户余额和双花，证券是撮合，选修暂停。红包雨是投放库存，不是社交群包。渠道、账本、日切文件是 Ch24，本场只调冻结 / 入账 / 退款。秒杀 remaining count 是近亲（Ch21），本章多了金额维。本场把拆包、原子库存和退回对账讲透。」问太多超过 10 分钟也是 red flag。黄金线还是那条：**问关键问题 → 自己给假设 → 写白板 → 继续。**",
      ].join("\n"),
    },
    {
      id: "sec-estimate",
      heading: "粗估",
      secNum: "26.2",
      related: ["ch03"],
      body: [
        "公式细节在 Ch03。这里只要数量级，证明你知道 **这题 bottleneck 是单包热点超发和一分钱对不齐，不是「全国红包 QPS」。** 下面用白板做**教学假设**，不是某厂内部数字，也不是某晚高峰公开峰值。",
        "",
        "假设：日发送约 **100 万个** 群红包；每包个数 N 教学 **10～100**（社交群常见量级，不是红包雨）；支付成功才可抢。",
        "",
        "| 项 | 怎么估 | 量级（教学假设） |",
        "|---|---|---|",
        "| 日均抢 | 100 万包 × 约 10 次领取 / 86400 | **约百级 QPS**；峰值按活动再乘，仍先报假设 |",
        "| 单包热点 | 同群同时点同一个 `packet_id` | **这才是库存锁要挡的**；全国总和不是得分点 |",
        "| 预生成存储 | 每包 N 个整数分 + 元信息 | **KB 级 / 包**；List 很短，不怕 |",
        "| 领取记录 | `(packet_id, user_id)` 一行 | 跟领取次数同数量级；按 packet 分片（Ch41） |",
        "| 对比「除夕峰值」宣传 | 面试现场不要编某晚 QPS | **本场不靠吞吐得分** |",
        "",
        "日常群红包的全球加总很小。贵的是：**同一个包的剩余个数 / 剩余金额不能被抢成负数**，以及 **已领合计 + 剩余 = 总额**。把入口画成十万 QPS 却用 `SELECT … FOR UPDATE` 锁那一行，是把秒杀题的错方案搬过来。",
        "",
        "**面试怎么说：**",
        "",
        "> 「百万包/天白板：日均抢百级。时间花在单包原子库存、拆包和退回对账上。不会拿某晚公开峰值当内部数。」",
        "",
        "常见算错：把红包雨 / 春节活动峰值当成普通群包；或只报发包、假装领取和过期退款不写库。教学用数量级，并标**假设**。",
      ].join("\n"),
    },
    {
      id: "sec-arch",
      heading: "高层架构",
      secNum: "26.3",
      related: ["ch38", "ch41", "ch24"],
      body: [
        "从左到右只画 **一条抢包控制面**：Client → Packet API → Redis 库存。不要在这张图上扇出钱包、渠道、风控、红包雨。钱在 Ch24，本图只放业务库存。面试官 buy-in 之后再挖拆法和过期。",
        "",
        d2(`
direction: right
app.class: go
app: "Client"
api.class: step
api: "Packet API"
rds.class: store
rds: "Redis 库存"
app -> api -> rds
`),
        "",
        "**本图引用**：Ch38 缓存与 CDN · Ch41 复制、分片、事务",
        "",
        "**写路径（发）：** 校验总额、个数、下限 1 分 → 落 `packets` 行 `pending_pay`（`pay_no`、`total_cents`、`total_n`、`expire_at`）→ 调支付冻结总额（Ch24）。**回调成功之前不能抢。** 支付成功：预生成 N 份（或均分）→ 灌 Redis（List 金额 + Hash 剩余个数/金额 + 过期 TTL）→ CAS `open`。TTL 略长于业务过期，避免包还在抢 cache 先没了（Ch38）。",
        "",
        "**写路径（抢）：** 鉴权 + 同群可见点到 → **Lua 原子预扣**（查已领、扣剩余、弹出金额、记下 user）→ 立刻 200 金额 → Outbox 落 `grabs` 行。不要在抢路径上 `FOR UPDATE` 红包行，也不要先写 DB 再扣 Redis。",
        "",
        "**读路径：** 红包封面（剩余个数）、领取列表。读可以走 Redis Hash / 领取 Set；DB 是对账权威。不是 Feed，不必 CDN。",
        "",
        "**schema（够用就停）：** `packets`（packet_id、sender_id、total_cents、total_n、remain_cents、remain_n、status、expire_at、pay_no）；`grabs`（packet_id、user_id UNIQUE、amount_cents、grabbed_at）；`outbox`。金额 **整数分**。不要在白板上画手气最佳动画表。",
        "",
        "Redis 这里是 **库存引擎**，不是 cache-aside 读加速（Ch38）：权威窗口内 remaining 以 Lua 为准；DB 靠 Outbox 追上，再用对账收口。秒杀 remaining count 是近亲（Ch21）——本章多一维 **剩余金额**，并且每一分都要对回支付。",
        "",
        "高层图到这里就该停，问一句：「方向 OK 吗？接下来挖怎么拆，然后是 Lua 防超发，最后过期退回和支付怎么切。」",
        "",
        "**面试怎么说：**",
        "",
        "> 「Client 打 Packet API，库存在 Redis。支付成功才灌包。账本不画进这张图。」",
      ].join("\n"),
    },
    {
      id: "sec-split",
      heading: "深入 · 怎么拆：预生成 vs 现场随机",
      secNum: "26.4",
      related: [],
      body: [
        "第一个 hard part。总额固定、份数固定，拆完必须 **加起来恰好等于总额**，每人至少 1 分。拼手气还要「看起来随机」，期望上别让先抢的稳赚。",
        "",
        "**算法（拼手气）：二倍均值。** 剩余金额 M、剩余人数 N，下一份在 `[1, min(2×(M/N), M-(N-1))]` 里随机，最后一份拿剩余。均分：`total/N`，余数分摊到若干份。全部用整数分，禁止 float。",
        "",
        "2026 白板默认：**发的时候就算完 N 份，打乱后放进 List。** 现场每次用二倍均值只当对照。",
        "",
        d2(`
grid-columns: 2
pre: {
  label: "预生成 · 默认"
  class: groupOk
  grid-columns: 2
  a.class: ok
  a: "发时算完"
  b.class: ok
  b: "抢时 LPOP"
}
live: {
  label: "现场随机"
  class: group
  grid-columns: 2
  c.class: step
  c: "二倍均值"
  d.class: step
  d: "只存剩余"
}
`),
        "",
        "| | 预生成（**默认**） | 现场随机（二倍均值） |",
        "|---|---|---|",
        "| 何时算金额 | **发红包 / 支付成功时** | 每一次抢 |",
        "| 抢路径 | List `LPOP` + 扣剩余 | 读 M、N → 随机 → 写回 M、N |",
        "| 总额 | 生成时就加总校验 | 最后一份吃剩余；并发必须进同一段 Lua |",
        "| 公平 | **打乱** 后，抢的次序 ≠ 生成次序 | 先抢的方差更大（算法本身如此） |",
        "| 内存 | N 个整数，群包可忽略 | 只存剩余，更省 |",
        "| 适合 | **高并发抢**；计算离开热路径 | 包很大、不想存 List；或要动态改区间 |",
        "",
        "预生成之后 **shuffle** 一句必讲：二倍均值按顺序生成时，前几份方差大；不打乱，先点的人吃的是「生成顺序」而不是「随机队列」。现场随机省掉 List，但随机仍在高峰 Lua 里，调试「为什么超发 / 为什么最后一份离谱」更难。",
        "",
        "均分不要另搞一套库存：List 里 N 个相同（或差 1 分）的数，抢路径与拼手气相同。不要均分走 DB、拼手气走 Redis。",
        "",
        "**面试怎么说：**",
        "",
        "> 「默认发时二倍均值预生成，打乱进 List。抢只 LPOP。现场随机能省内存，但热路径更重，当对照。」",
        "",
        "trade-off：预生成换内存和「发的时候多一次 CPU」，抢变纯库存；现场随机换省 List，把随机和扣库存绑在一次原子里。两种都要 **原子**，都不是「先 SELECT 剩余再 UPDATE」。网上只背二倍均值公式、不说预生成，只答了算法没答系统。",
      ].join("\n"),
    },
    {
      id: "sec-stock",
      heading: "深入 · 库存原子防超发",
      secNum: "26.5",
      related: ["ch38", "ch41"],
      body: [
        "第二个 hard part。库存是两维：**剩余个数** 和 **剩余金额**，必须一起扣。只 DECR 个数、金额另算，会出现「人数对了、钱多发了」。超发在红包里不可接受——商品秒杀偶发超卖还能补货，这里多发的是真钱（公开面试常拿这点和普通秒杀对比）。",
        "",
        "2026 默认：高峰 **Lua 一段脚本** 做完校验 + 预扣，不要给整个包加 SETNX 大锁，更不要 `FOR UPDATE` 那一行。Lua 在 Redis 单线程里就是原子；大锁把同包请求排成一串，锁过期还会双扣。",
        "",
        d2(`
direction: right
chk.class: go
chk: "查已领"
lua.class: step
lua: "Lua 预扣"
pop.class: step
pop: "出金额"
async.class: ok
async: "异步落库"
chk -> lua -> pop -> async
`),
        "",
        "**本图引用**：Ch38 缓存与 CDN · Ch41 复制、分片、事务",
        "",
        "脚本里按顺序（一次 eval，不要拆成客户端多条）：",
        "",
        "1. 已领过 → 返回已领金额（幂等），不要再扣。",
        "2. `remain_n <= 0` 或 List 空 → 抢完。",
        "3. 预生成：`LPOP` 一份；现场随机：按 M、N 算一份。",
        "4. `remain_n -= 1`，`remain_cents -= amount`（两维一起）。",
        "5. 记下 `user → amount`（Hash / Set）。返回金额。",
        "",
        d2(`
shape: sequence_diagram
usr: "User"
api: "Packet"
rds: "Redis"
ob: "Outbox"
usr -> api: "抢"
api -> rds: "Lua 预扣"
rds -> api: "金额"
api -> usr: "200"
api -> ob: "异步记"
`),
        "",
        "**本图引用**：Ch41 复制、分片、事务",
        "",
        "参与者停在四个：User / Packet / Redis / Outbox。DB 不进抢的同步路径。",
        "",
        "**双写：** Lua 成功是抢路径权威；`grabs` 行异步落。失败就重试同一条 Outbox，靠 `(packet_id, user_id)` UNIQUE 挡住双行。Lua 里已经记下 user，重试抢会走步骤 1，不会把库存扣第二次。Redis 丢了：用 DB 已领合计重建 remaining，**宁可少卖一会儿，不要超发**；重建窗口拒抢或只读。",
        "",
        "内部不变量（对账用，不必每抢扫全表）：`sum(grabs.amount) + remain_cents = total_cents`，且 `count(grabs) + remain_n = total_n`。打破就是超发或少记，进补偿，不要默默改 Hash。",
        "",
        "和 Ch21 秒杀库存的差别一句：**那边常是件数；这边件数和金额都是库存，并且金额要对回账本。** 不要把订单章的 DB CAS 预扣当群抢第一答案——QPS 在单 key 上，行锁会排成队。",
        "",
        "**面试怎么说：**",
        "",
        "> 「剩余个数和剩余金额一起 Lua 预扣。先扣缓存，Outbox 落库。UNIQUE 防双领。高峰不锁 DB 行。」",
        "",
        "trade-off：Redis 窗口内快、换来双写和对账；全程 DB 行锁正确但抢路径会被单包打穿。客户端「GET 剩余、再 DECR、再 LPOP」不是原子，是 red flag。",
      ].join("\n"),
    },
    {
      id: "sec-refund",
      heading: "深入 · 过期退回与支付对账边界",
      secNum: "26.6",
      related: ["ch24", "ch25", "ch41"],
      body: [
        "第三个 hard part。没抢完的钱必须在过期后 **原路退回发送方**，一分不能少、不能退两次。红包系统算出「还剩多少」；**真正动钱的是支付 / 账本（Ch24）**。过期怎么叫醒，点到 Ch25，不要把调度器画成本章主架构。",
        "",
        d2(`
direction: right
sch.class: go
sch: "Scheduler"
frz.class: step
frz: "冻剩余"
pay.class: step
pay: "Pay 退款"
led.class: store
led: "Ledger"
sch -> frz -> pay -> led
`),
        "",
        "**本图引用**：Ch24 支付系统 · Ch41 复制、分片、事务",
        "",
        "**过期怎么跑：** 发的时候带 `expire_at`（教学 24h）。Ch25 延迟任务或扫描到期 `open` 包。抢与过期抢同一段剩余：先 Lua / CAS 把包打成 `expiring`，**剩余清零**（之后 Lua 再抢失败）。然后用 `remain_cents` 调 Ch24 退款，幂等键 `packet_id` + 过期后缀。退款成功再 `expired`。退款 at-least-once：第二次 UNIQUE 命中直接成功，不要再贷一笔。",
        "",
        "剩余已经是 0：只改状态，不调退款。部分领取：退的是 **当时冻住的剩余**，不是总额。过期后晚到的抢：库存已冻，返回抢完；不要为了体验把过期包又打开。",
        "",
        d2(`
grid-columns: 2
pkt: {
  label: "本章 · 红包"
  class: groupOk
  grid-columns: 2
  a.class: ok
  a: "个数库存"
  b.class: ok
  b: "剩余金额"
}
pay: {
  label: "Ch24 · 支付"
  class: group
  grid-columns: 2
  c.class: step
  c: "冻结入账"
  d.class: step
  d: "退款对账"
}
`),
        "",
        "**本图引用**：Ch24 支付系统",
        "",
        "| 动作 | 红包侧 | 支付 / 账本侧（Ch24） |",
        "|---|---|---|",
        "| 发 | `pending_pay`，未灌库存 | 冻结 / 扣发送方 `total_cents` |",
        "| 支付成功 | 预生成、灌 Redis、`open` | 支付单 success；钱在平台托管口径 |",
        "| 抢 | Lua 扣剩余；Outbox 记 grab | **异步** 入账到领取人；不 2PC |",
        "| 过期 | 冻 remaining，停抢 | 按剩余退回发送方 |",
        "| 对账 | 已领合计 + 剩余 ≟ 总额 | 冻结 ≟ 已入账领取 + 已退回 + 在途 |",
        "",
        "对账两刀，别混：",
        "",
        "1. **库存自检**（本章）：已领金额 + remaining = total；已领个数 + remain_n = total_n。",
        "2. **资金对账**（Ch24）：渠道 / 账本里，这条 `pay_no` 的冻结，要等于领取入账 + 过期退款 + 在途。差了走补入账或冲正，**不改历史流水**，也不要再扣一次发送方。",
        "",
        "不要和支付做 2PC / XA：抢成功先承诺库存，入账走 Outbox（Ch41）。入账慢可以，超发不行。积压用重试 + DLQ；对账作业本身幂等（同一 `packet_id` 日切重跑不双退）。钱包余额怎么防双花，选修暂停，本场不画。",
        "",
        "**面试怎么说：**",
        "",
        "> 「过期先冻剩余再调退款，refund_id 幂等。红包管库存，Ch24 管钱。库存一刀、账本一刀，对不上走补偿。」",
        "",
        "trade-off：异步入账让抢路径短，换来「已抢未入账」窗口，靠 Outbox + 对账收口。同步 RPC 支付再返回抢结果，p99 绑死账本，高峰更糟。把日切 CSV 解析画进红包章，是把 Ch24 整段搬进来。",
      ].join("\n"),
    },
    {
      id: "sec-2026",
      heading: "2026 vs 网上旧答",
      secNum: null,
      related: [],
      body: [
        "<details>",
        "<summary>无 Xu 专章 · 不要把行锁 / 某晚 QPS 当第一答案</summary>",
        "",
        "Alex Xu 两卷没有「设计红包系统」专章。网上旧答大量是：每抢 `FOR UPDATE` 一行、只背二倍均值公式、SETNX 锁整个包、把钱包和渠道清算画进第一张图、或把 InfoQ 2017 微信红包公开峰值当自己的事实。**正文按 2026 国内公开「设计红包 / 微信红包」面试重写**，不是笔记润色，也不是某厂内部架构课。",
        "",
        "| 网上旧答 / 早期工程 | 现在怎么答 |",
        "|---|---|---|",
        "| 只讲二倍均值，不讲预生成 | **默认发时预生成 + 打乱**；现场随机列表对照 |",
        "| 高峰 `SELECT … FOR UPDATE` | **Lua 原子预扣**；DB 不进抢的同步路径 |",
        "| SETNX 锁整个红包 | Lua 已原子；大锁排队、TTL 还会双扣 |",
        "| 客户端 GET / DECR / LPOP 分步 | **一条 Lua**；分步会超发 |",
        "| 只扣个数，金额另算 | **剩余个数 + 剩余金额** 一起扣 |",
        "| 乐观锁 version 抢包 | 资金场景冲突回滚体验差；库存放到 Redis |",
        "| 2017 排队 / memcached CAS 当白板终图 | 知道「别让 DB 行锁吃高峰」；2026 默认 Lua + `packet_id` |",
        "| 第一张图画钱包 / 渠道 / 红包雨 | **Client → Packet API → Redis**；钱回 Ch24 |",
        "| 和支付 2PC / TCC 当默认 | **本地库存 + Outbox 入账**；退款幂等 |",
        "| 过期扫全表无锁双退 | Ch25 到期任务；冻剩余 + `refund_id` |",
        "| 某晚高峰 QPS / 内部 SET 数 | **禁止**；只用教学数量级 |",
        "",
        "仍成立的骨架：总额不能超、每人一次、过期退剩余、信息和资金分开、对账收口最终一致。过时的是把 DB 行锁、大锁和支付中台当红包题的第一张图。公开 2017 方案强调请求排队避免 DB 并发抢锁——方向（高峰别打行锁）仍对，手段换成 Redis 库存更适合现在的白板。",
        "",
        "</details>",
      ].join("\n"),
    },
    {
      id: "sec-traps",
      heading: "追问陷阱",
      secNum: null,
      related: ["ch24", "ch38", "ch41"],
      body: [
        "1. **「和支付系统什么关系？」** → 红包是业务库存。冻结、领取入账、过期退款是 Ch24。不重画账本和渠道。",
        "2. **「和秒杀库存呢？」** → 都是原子预扣。秒杀常是件数（Ch21）；红包多金额维，还要对回账。",
        "3. **「为什么预生成而不是现场随机？」** → 抢路径只出队；总额生成时就校验；打乱后次序更公平。现场随机当对照。",
        "4. **「二倍均值怎么保证加总？」** → 整数分；最后一份吃剩余；预生成结束 assert sum。",
        "5. **「为什么不锁 DB 行？」** → 同包热点会排队。Lua 扣库存，DB 异步落。",
        "6. **「为什么不用 SETNX？」** → 整包串行，锁过期双扣。Lua 本身原子。",
        "7. **「只 DECR 个数行不行？」** → 不行。金额会超发或少发。两维一起扣。",
        "8. **「Lua 成功、落库失败？」** → Outbox 重试；UNIQUE 防双行。再抢走已领分支。",
        "9. **「同人点两次？」** → Lua 先查已领；DB `(packet_id, user_id)` UNIQUE。",
        "10. **「过期和最后一抢同时？」** → 都走冻剩余 / Lua。谁先谁赢；退的是冻住的剩余。",
        "11. **「为什么还要对账？」** → 双写和异步入账会暂时不平。库存一刀、账本一刀。",
        "12. **「要不要 2PC 到支付？」** → 不要。库存本地提交 + Outbox。2PC 是 red flag。",
        "13. **终图已经很大了还往上堆？** → 红包雨、钱包、渠道清算、微信 SET 化、某晚 QPS 都不是本章第一答案。讲透三条 hard part 比画 20 个框得分高。",
      ].join("\n"),
    },
    {
      id: "sec-next",
      heading: "wrap-up 与下一步",
      secNum: null,
      related: ["ch27"],
      body: [
        "收尾不要说完美。三个 bottleneck 口播：",
        "",
        "| bottleneck | 你怎么接 |",
        "|---|---|",
        "| 拆不平 / 先抢稳赚 | 整数分；二倍均值；**预生成 + 打乱**；现场随机当对照 |",
        "| 超发或双领 | Lua 同时扣剩余个数和金额；已领短路径；UNIQUE；高峰不锁 DB 行 |",
        "| 过期退不对、和账对不上 | 先冻剩余再幂等退款（Ch24）；库存自检 + 资金对账；Outbox 不 2PC |",
        "",
        "自测：合上这一页，用 30 秒开场 + 白板 Client → Packet API → Redis，把预生成 vs 现场、Lua 两维库存、过期冻剩余、和 Ch24 的切法讲给空气听。哪句卡，回哪一节。不要开口重讲账本科目，不要画钱包。",
        "",
        "下一道题是 **Ch27 · 即时配送**。红包把「有限份数的钱怎么拆、怎么不超发」讲完；配送换成 **接单匹配、运力、实时位置**——库存变成的是人在路上，不是 List 里的分。",
      ].join("\n"),
    },
  ],
  reviewMd: `# Ch26 · 记忆闪卡

| # | 正面 | 背面 |
|---|---|---|
| 1 | 红包三个 hard part？ | 怎么拆（预生成 vs 现场随机）；库存原子防超发；过期退回 + 和支付对账边界。 |
| 2 | 默认范围？ | **群红包**：固定总额、限个数、拼手气或均分、过期退剩余。不是钱包、不是证券、不重写 Ch24。 |
| 3 | 2026 默认怎么拆？ | **发时二倍均值预生成 + 打乱** 进 List。现场随机只存剩余，当对照。 |
| 4 | 二倍均值一句？ | 剩余 M、N 人，下一份 \`[1, min(2×M/N, 保证后人至少 1 分)]\`；最后一份吃剩余。整数分。 |
| 5 | 库存是几维？ | **剩余个数 + 剩余金额**，Lua 里一起扣。只 DECR 个数会超发钱。 |
| 6 | 高峰为什么不锁 DB 行？ | 单 \`packet_id\` 热点会排队。抢路径 Lua；DB Outbox 异步落。 |
| 7 | 为什么不用整包 SETNX？ | Lua 已原子。大锁串行；TTL 过期可能双扣。 |
| 8 | 双写怎么收？ | Lua 是抢路径权威；\`grabs\` UNIQUE；失败重试走已领。对账：已领 + 剩余 = 总额。 |
| 9 | 过期退回步骤？ | Scheduler 到期 → 冻 remaining 停抢 → Ch24 按剩余退款（\`refund_id\` 幂等）。 |
| 10 | 和 Ch24 怎么切？ | 红包：业务库存。支付：冻结、领取入账、退款、日切账本。不 2PC。 |
| 11 | 和秒杀库存怎么切？ | 都是原子预扣。秒杀常是件数（Ch21）；红包多金额维 + 资金对账。 |
| 12 | 这题最大的 over-engineering？ | 行锁、钱包、渠道清算、红包雨、某晚 QPS。讲透三条 hard part。 |`,
});
